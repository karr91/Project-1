<!DOCTYPE html>
<html lang="en">
<%- include( '../partials/head.ejs' , {page: photo.name} ) %>

    <body>
        <%- include( '../partials/nav' ) %>
            <section class="title-sec">
                <div class="title-body">
                    <p class="title"><%photo.name%></p>
                </div>
            </section>
            <main class="showPhoto">
                <div class="show">
                    <article class="card">
                        <section class="card-image">
                            <figure class="image is-10by10">
                                <img src="<%= photo.image %>" alt="Placeholder image" />
                            </figure>
                        </section>
                        <div class="card-content">
                            <div class="edit">
                                <a class="button is-info" href="/photos/<%= photo.id %>/edit">Edit Photo
                                </a>
                            </div>
                        </div>
                    </article>
                </div>
            </main>
    </body>

</html>